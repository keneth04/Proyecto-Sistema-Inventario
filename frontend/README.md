# Frontend

Estado actual: base en transición tras limpieza arquitectónica (Fase 2).

## Requisitos
- Node 18+
- Backend en `http://localhost:3000`

## Instalación y ejecución
```bash
cd frontend
npm install
npm run dev
```

## Variables
- `VITE_API_BASE` (opcional): por defecto usa `/api`.
